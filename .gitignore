node_modules
.next
out
build
artifacts
.env
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
.deployer.json

# Vendored Tesseract runtime — regenerate with `npm run vendor:ocr`
public/tesseract
.pending-deposit.json
.sponsor.json
.env*

# Recording sandbox — local only, never shipped or committed.
app/demo/
components/demo/

# Test for the local-only recording sandbox — imports components/demo, which is ignored.
tests/demo-figures.test.mjs
